<resources>
    <!-- Base application theme. -->
    <style name="Theme.Florist" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:autoCompleteTextViewStyle">@drawable/rounded_normal_edittext</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/main_color</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo_padded</item>

        <item name="postSplashScreenTheme">@style/Theme.Florist</item>
    </style>

    <style name="LoginTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@android:color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:dropDownSelector">@drawable/rounded_normal_edittext</item>
    </style>
    <style name="CornerCut.Medium" parent="">
        <item name="cornerFamily">rounded</item> <item name="cornerSize">12dp</item> </style>

</resources>